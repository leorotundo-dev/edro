import ContatosClient from './ContatosClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Contatos — Edro' };

export default function ContatosPage() {
  return <ContatosClient />;
}
