import JarvisProposalsClient from './JarvisProposalsClient';

export const metadata = {
  title: 'Jarvis — Propostas | Edro Studio',
};

export default function JarvisPage() {
  return <JarvisProposalsClient />;
}
