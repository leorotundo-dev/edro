╔════════════════════════════════════════════════════════════════════════════╗
║                    SISTEMA DE EDITAIS - MEMODROPS                          ║
║                        Arquitetura Completa                                ║
╚════════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────────┐
│                           CAMADA DE APRESENTAÇÃO                           │
│                          (Frontend - Next.js/React)                        │
└────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
    │  Lista Editais  │    │  Criar Edital   │    │ Detalhes Edital │
    │  /editais       │    │  /editais/novo  │    │  /editais/[id]  │
    └────────┬────────┘    └────────┬────────┘    └────────┬────────┘
             │                      │                       │
             │  ┌─────────────────────────────────────┐   │
             └──┤    Filtros, Dashboard, CRUD         ├───┘
                └───────────────┬─────────────────────┘
                                │
                                │ HTTP/REST
                                │
┌───────────────────────────────▼─────────────────────────────────────────┐
│                         CAMADA DE API (Backend)                         │
│                          (Fastify + TypeScript)                         │
└─────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────────────────────────────┐
    │                    ROTAS (routes/editais.ts)              │
    ├───────────────────────────────────────────────────────────┤
    │  GET    /api/editais                    → Listar          │
    │  GET    /api/editais/:id                → Buscar          │
    │  POST   /api/editais                    → Criar           │
    │  PUT    /api/editais/:id                → Atualizar       │
    │  DELETE /api/editais/:id                → Deletar         │
    │                                                            │
    │  GET    /api/editais/:id/eventos        → Eventos         │
    │  POST   /api/editais/:id/eventos        → Criar Evento    │
    │  GET    /api/editais/:id/usuarios       → Interessados    │
    │  POST   /api/editais/:id/interesse      → Add Interesse   │
    │                                                            │
    │  GET    /api/editais/reports/by-status  → Relatório       │
    │  GET    /api/editais/reports/by-banca   → Relatório       │
    └──────────────────────┬────────────────────────────────────┘
                           │
                           │ Chama Repository
                           │
    ┌──────────────────────▼─────────────────────────────────────┐
    │         REPOSITORY (repositories/editalRepository.ts)      │
    ├────────────────────────────────────────────────────────────┤
    │  • EditalRepository                                        │
    │    - findAll(filters)                                      │
    │    - findById(id)                                          │
    │    - create(data)                                          │
    │    - update(id, data)                                      │
    │    - delete(id)                                            │
    │    - getStats()                                            │
    │    - findEventosByEditalId(id)                             │
    │    - createEvento(data)                                    │
    │    - findUsuariosByEditalId(id)                            │
    │    - addUsuario(editalId, userId)                          │
    │    - getProximasProvas()                                   │
    └──────────────────────┬─────────────────────────────────────┘
                           │
                           │ SQL Queries
                           │
┌───────────────────────────▼──────────────────────────────────────────────┐
│                      CAMADA DE DADOS (PostgreSQL)                        │
└──────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────────┐
    │                    TABELA: editais                         │
    ├────────────────────────────────────────────────────────────┤
    │  id                 UUID PRIMARY KEY                       │
    │  codigo             TEXT UNIQUE                            │
    │  titulo             TEXT                                   │
    │  orgao              TEXT                                   │
    │  banca              TEXT                                   │
    │  status             ENUM (6 valores)                       │
    │  data_publicacao    DATE                                   │
    │  data_prova_prevista DATE                                  │
    │  numero_vagas       INTEGER                                │
    │  cargos             JSONB                                  │
    │  disciplinas        JSONB                                  │
    │  tags               JSONB                                  │
    │  created_at         TIMESTAMPTZ                            │
    │  updated_at         TIMESTAMPTZ                            │
    └────────────────────────┬───────────────────────────────────┘
                             │
              ┌──────────────┼──────────────┬─────────────────┐
              │              │              │                 │
    ┌─────────▼──────┐  ┌────▼────────┐  ┌─▼─────────────┐  │
    │ edital_eventos │  │edital_questoes│ │edital_usuarios│  │
    ├────────────────┤  ├──────────────┤  ├───────────────┤  │
    │ id             │  │ id           │  │ id            │  │
    │ edital_id   ───┼──┤ edital_id ───┼──┤ edital_id  ───┼──┘
    │ tipo           │  │ questao_id   │  │ user_id       │
    │ titulo         │  │ disciplina   │  │ cargo_int...  │
    │ data_inicio    │  │ peso         │  │ notific...    │
    │ concluido      │  └──────────────┘  └───────────────┘
    └────────────────┘

    ┌────────────────────────────────────────────────────────────┐
    │               VIEW: editais_stats (materializada)          │
    ├────────────────────────────────────────────────────────────┤
    │  Agrega estatísticas de cada edital:                       │
    │  • Total de usuários interessados                          │
    │  • Total de questões vinculadas                            │
    │  • Informações principais do edital                        │
    └────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

                           FLUXO DE DADOS

┌──────────┐         ┌──────────┐         ┌────────────┐         ┌──────┐
│ Usuário  │  HTTP   │   Web    │  REST   │   Backend  │  SQL    │  DB  │
│  Admin   ├────────►│  React   ├────────►│  Fastify   ├────────►│ Pg   │
└──────────┘         └──────────┘         └────────────┘         └──────┘
     ▲                     ▲                     ▲                    │
     │                     │                     │                    │
     └─────────────────────┴─────────────────────┴────────────────────┘
                          Resposta JSON

═══════════════════════════════════════════════════════════════════════════

                        ESTRUTURA DE ARQUIVOS

memodrops-main/
│
├── apps/backend/src/
│   ├── db/
│   │   ├── migrations/
│   │   │   └── 0014_editais_system.sql ◄─── Migration Principal
│   │   └── seed-editais.sql            ◄─── Dados de Exemplo
│   │
│   ├── types/
│   │   └── edital.ts                   ◄─── TypeScript Types
│   │
│   ├── repositories/
│   │   └── editalRepository.ts         ◄─── Lógica de Dados
│   │
│   └── routes/
│       ├── editais.ts                  ◄─── Endpoints API
│       └── index.ts                    ◄─── Registro de Rotas
│
├── apps/web/app/admin/editais/
│   ├── page.tsx                        ◄─── Lista de Editais
│   ├── novo/
│   │   └── page.tsx                    ◄─── Criar Edital
│   └── [id]/
│       └── page.tsx                    ◄─── Detalhes do Edital
│
├── SISTEMA_EDITAIS_README.md           ◄─── Doc Técnica Completa
├── GUIA_RAPIDO_EDITAIS.md              ◄─── Guia Rápido
├── SISTEMA_EDITAIS_RESUMO.md           ◄─── Resumo Executivo
├── SISTEMA_EDITAIS_ARQUITETURA.txt     ◄─── Este Arquivo
└── test-editais.ps1                    ◄─── Script de Testes

═══════════════════════════════════════════════════════════════════════════

                         STATUS DO EDITAL

    ┌────────────┐
    │ rascunho   │  Edital em elaboração
    └──────┬─────┘
           │
           ▼
    ┌────────────┐
    │ publicado  │  Edital oficial publicado
    └──────┬─────┘
           │
           ▼
    ┌────────────┐
    │em_andamento│  Inscrições abertas / Provas em curso
    └──────┬─────┘
           │
           ├──────┐
           │      │
           ▼      ▼
    ┌──────────┐ ┌───────────┐
    │concluido │ │ cancelado │  Estados finais
    └──────────┘ └───────────┘
           │
           ▼
    ┌──────────┐
    │ suspenso │  Estado temporário
    └──────────┘

═══════════════════════════════════════════════════════════════════════════

                      TIPOS DE EVENTOS

    📝 inscricao    ─► Período de inscrições
    📋 prova        ─► Data de aplicação das provas
    🎯 resultado    ─► Divulgação de resultados
    ⚖️ recurso       ─► Prazo para recursos
    📢 convocacao   ─► Convocação de aprovados
    📌 outro        ─► Outros eventos

═══════════════════════════════════════════════════════════════════════════

                    ENDPOINTS PRINCIPAIS (20+)

┌─────────────────────────────────────────────────────────────────────────┐
│                            EDITAIS                                      │
├─────────────────────────────────────────────────────────────────────────┤
│  GET    /api/editais              ─► Listar todos (com filtros)        │
│  GET    /api/editais/:id          ─► Buscar por ID                     │
│  POST   /api/editais              ─► Criar novo                        │
│  PUT    /api/editais/:id          ─► Atualizar                         │
│  DELETE /api/editais/:id          ─► Deletar                           │
│  GET    /api/editais-stats        ─► Estatísticas gerais               │
│  GET    /api/editais/:id/stats    ─► Estatísticas do edital            │
├─────────────────────────────────────────────────────────────────────────┤
│                           EVENTOS                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  GET    /api/editais/:id/eventos          ─► Listar eventos            │
│  POST   /api/editais/:id/eventos          ─► Criar evento              │
│  PUT    /api/editais/eventos/:eventoId    ─► Atualizar evento          │
│  DELETE /api/editais/eventos/:eventoId    ─► Deletar evento            │
├─────────────────────────────────────────────────────────────────────────┤
│                    USUÁRIOS INTERESSADOS                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GET    /api/editais/:id/usuarios         ─► Listar interessados       │
│  POST   /api/editais/:id/interesse        ─► Adicionar interesse       │
│  DELETE /api/editais/:id/interesse/:userId ─► Remover interesse        │
├─────────────────────────────────────────────────────────────────────────┤
│                          RELATÓRIOS                                     │
├─────────────────────────────────────────────────────────────────────────┤
│  GET    /api/editais/reports/by-status   ─► Contagem por status        │
│  GET    /api/editais/reports/by-banca    ─► Contagem por banca         │
│  GET    /api/editais/reports/proximas-provas ─► Próximas provas        │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

                     RECURSOS IMPLEMENTADOS

✅ CRUD Completo de Editais
✅ Sistema de Status (6 estados)
✅ Gestão de Cronograma (Eventos)
✅ Múltiplos Cargos por Edital
✅ Múltiplas Disciplinas por Edital
✅ Sistema de Tags
✅ Filtros Avançados
✅ Busca Textual
✅ Estatísticas em Tempo Real
✅ Relatórios Consolidados
✅ Interface Web Responsiva
✅ API RESTful Completa
✅ TypeScript Types
✅ Validações de Dados
✅ Tratamento de Erros
✅ Documentação Completa
✅ Testes Automatizados
✅ Dados de Exemplo
✅ Índices Otimizados
✅ View de Estatísticas

═══════════════════════════════════════════════════════════════════════════

                      TECNOLOGIAS UTILIZADAS

Backend:
  • Node.js
  • TypeScript
  • Fastify (framework web)
  • PostgreSQL (banco de dados)
  • SQL (queries otimizadas)

Frontend:
  • Next.js 14
  • React 18
  • TypeScript
  • Tailwind CSS
  • Client-side routing

Ferramentas:
  • PowerShell (scripts de teste)
  • SQL (migrations e seeds)
  • Git (controle de versão)

═══════════════════════════════════════════════════════════════════════════

                       PRÓXIMOS PASSOS

1. Executar migration:
   psql $env:DATABASE_URL -f src/db/migrations/0014_editais_system.sql

2. Inserir dados de exemplo:
   psql $env:DATABASE_URL -f src/db/seed-editais.sql

3. Iniciar backend:
   cd apps/backend && npm run dev

4. Iniciar frontend:
   cd apps/web && npm run dev

5. Acessar interface:
   http://localhost:3000/admin/editais

6. Testar API:
   .\test-editais.ps1

═══════════════════════════════════════════════════════════════════════════

                     MEMODROPS - SISTEMA DE EDITAIS
                    Sistema Completo de Gestão de Editais
                     Desenvolvido com ❤️ para Educação

═══════════════════════════════════════════════════════════════════════════
